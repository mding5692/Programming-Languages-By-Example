/* don't need to declare what type of variable
just the reserved word var is enough */

var integer = 1;
var str = "hello";

var boole = true;

// same for constants
const oneNum = 1;

// when javascript is confused it prints out NaN (not a number) or undefined
var notANum = NaN;

var haveNoIdea = undefined;
